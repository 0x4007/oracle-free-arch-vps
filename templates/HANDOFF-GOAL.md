# Agent Handoff Goal

Replace `<KIT_PATH>` with the absolute local path after unpacking. Send the
following single sentence to the primary agent:

Goal: Read `<KIT_PATH>/AGENTS.md` and every numbered runbook in `<KIT_PATH>` in full, act as the sole orchestrator and infrastructure writer, verify current Oracle policy and live account state, then build and live-accept one running Oracle Always Free A1 machine named `arch` with Arch Linux ARM, 2 OCPUs, 12 GB RAM, the 50 GB staging boot plus 150 GB Arch root design, and an accepted paired safety-backup set, while obtaining every exact approval required by the kit and keeping all credentials and identifiers out of shared reports.
